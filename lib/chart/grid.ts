import type { GridComponentOption } from 'echarts'

const defaultGridConfig: GridComponentOption = {
  containLabel: true,
  left: 16,
  top: 35,
  bottom: 16,
  right: 32,
}

export { defaultGridConfig }
