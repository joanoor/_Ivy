import type { TitleComponentOption } from 'echarts'

const defaultTitleConfig: TitleComponentOption = {
  type: 'plain',
  show: true,
  left: 'center',
  right: 'auto',
  bottom: 'auto',
  width: '100%',
  textStyle: {
    fontSize: '10',
  },
}

export { defaultTitleConfig }
